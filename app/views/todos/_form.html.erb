<%= form_for @todo, url: {action: "create"},  html: {class: "ui form"} do |f|%>
	<%= f.text_field :title %>
	<%= f.datetime_select :due_date %>
	<%= f.select :priority, (1...4) %>
	<%= f.submit 'Add Task' %>
<% end %>
